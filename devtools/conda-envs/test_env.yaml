name: test
channels:
dependencies:
    # Base depends
  - python
  - pip
  - pandas

    # Testing
  - pytest
  - pytest-cov
  - codecov

    # Pip-only installs
  - pip:
  #  - codecov
    - bravado

